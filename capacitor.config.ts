const config = {
  appId: 'com.aurapulse.musicplayer',
  appName: 'AuraPulse Music Player',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
    cleartext: true
  }
};

export default config;
